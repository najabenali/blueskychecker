{
  "name": "blueskychecker",
  "version": "1.0.0",
  "scripts": {
    "start": "node api/server.js",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "frontend": "cd src && react-scripts start",
    "backend": "node api/server.js",
    "build": "cd src && react-scripts build"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
